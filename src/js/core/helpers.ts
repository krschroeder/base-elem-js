
// Dom shortcuts
const 
    w       = window,
    d       = document,
    body    = d.body,
    root    = d.documentElement,
    oa      = Object.assign,
    af      = Array.from,
    isArr   = Array.isArray,
    isStr = (str: any) => typeof str === 'string'
;

export {
    w,
    d, 
    body,
    root,
    oa, 
    af, 
    isArr, 
    isStr
}