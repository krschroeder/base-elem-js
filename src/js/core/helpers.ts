
// Dom shortcuts
const 
    d       = document,
    body    = d.body,
    oa      = Object.assign,
    af      = Array.from,
    isArr   = Array.isArray,
    isStr = (str: any) => typeof str === 'string'
;

export {
    d, 
    body,
    oa, 
    af, 
    isArr, 
    isStr
}